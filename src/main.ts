import { createApp } from 'vue'
// import App from './App.vue'

// const img = require('./assets/logo.png') // eslint-disable-line

import App from './App'

// h函数相当于react中的createElement
createApp(App).mount('#app')
